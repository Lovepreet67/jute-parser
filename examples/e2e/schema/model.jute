include "common.jute";

module serialization {
    class Person {
        int id;
        string name;
        boolean active;
        common.Address address;
    }

    class Company {
        int id;
        Person owner;
        vector<Person> employees;
        map<string, Person> directory;
    }
}
