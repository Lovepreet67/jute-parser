# Full Jute Example

This example demonstrates the complete Jute workflow:

- Parsing multiple `.jute` files
- Resolving `include` statements
- Generating Rust code
- Serializing and deserializing complex data structures

## Run

```bash
# make sure you are in e2e dir
cargo run 
```
